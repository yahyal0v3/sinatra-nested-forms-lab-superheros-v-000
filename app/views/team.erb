<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Teams</title>
  </head>
  <body>
    <h1><%= @team.name %></h1>
    <% @heroes.each do |hero| %>
    <h2>Hero Name:<%= hero.name %></h2>
    Hero Power:<%= hero.power %>
    Hero Biography:<%= hero.bio %>
    <% end %>
  </body>
</html>
